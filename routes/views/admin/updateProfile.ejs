<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mettre à jour le Profil</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">

    <!-- ✅ Fixed Navbar -->
    <%- include('../admin/partials/navbar') %>


    <!-- ✅ Main Content with Padding -->
    <main class="pt-24">
        <section class="container mx-auto my-12 px-6">
            <h2 class="text-3xl font-bold text-center">Mettre à jour votre profil</h2>

            <!-- Profile Update Form -->
            <div class="max-w-md mx-auto bg-white p-6 rounded-lg shadow-lg mt-6">
                <form action="/admin.faces/update-profile" method="POST">
                    <div class="mb-4">
                        <label for="nom" class="block text-sm font-semibold">Nom</label>
                        <input type="text" name="nom" id="nom" value="<%= user.nom %>" class="w-full px-4 py-2 border border-gray-300 rounded-lg mt-1">
                    </div>
                    <div class="mb-4">
                        <label for="email" class="block text-sm font-semibold">Email</label>
                        <input type="email" name="email" id="email" value="<%= user.email %>" class="w-full px-4 py-2 border border-gray-300 rounded-lg mt-1">
                    </div>
                    <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-lg">Mettre à jour</button>
                </form>
            </div>
        </section>
    </main>

</body>
</html>
